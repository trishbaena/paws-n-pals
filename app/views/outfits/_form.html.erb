<%= simple_form_for @outfit do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :photo_url %>
  <%= f.input :price %>
  <%= f.submit @outfit.new_record? ? "Create Outfit" : "Update Outfit" %>
<% end %>
